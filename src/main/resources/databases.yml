database:
  type: SQLite
  mysql:
    host: localhost
    port: 3306
    database: auth
    username: root
    password: ""
    useSSL: false
    pool:
      max-pool-size: 10
      min-idle: 2
      connection-timeout: 5000
      idle-timeout: 600000
      max-lifetime: 1800000
  sqlite:
    filename: "auth.db"